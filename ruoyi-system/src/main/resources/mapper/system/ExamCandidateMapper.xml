<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ExamCandidateMapper">

    <resultMap type="ExamCandidate" id="ExamCandidateResult">
        <id     property="candidateId"    column="candidate_id"    />
        <result property="candidateName"  column="candidate_name"  />
        <result property="idCard"         column="id_card"         />
        <result property="phone"          column="phone"           />
        <result property="gender"         column="gender"          />
        <result property="email"          column="email"           />
        <result property="status"         column="status"          />
        <result property="userId"          column="user_id"         />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
        <result property="remark"         column="remark"          />
    </resultMap>

    <sql id="selectExamCandidateVo">
        select candidate_id, candidate_name, id_card, phone, gender, email, status, user_id, create_by, create_time, update_by, update_time, remark
        from exam_candidate
    </sql>

    <select id="selectExamCandidateList" parameterType="ExamCandidate" resultMap="ExamCandidateResult">
        <include refid="selectExamCandidateVo"/>
        <where>
            <if test="candidateName != null and candidateName != ''">
                AND candidate_name like concat('%', #{candidateName}, '%')
            </if>
            <if test="idCard != null and idCard != ''">
                AND id_card = #{idCard}
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectExamCandidateAll" resultMap="ExamCandidateResult">
        <include refid="selectExamCandidateVo"/> where status = '0'
    </select>

    <select id="selectExamCandidateById" parameterType="Long" resultMap="ExamCandidateResult">
        <include refid="selectExamCandidateVo"/> where candidate_id = #{candidateId}
    </select>

    <select id="selectExamCandidateByUserId" parameterType="Long" resultMap="ExamCandidateResult">
        <include refid="selectExamCandidateVo"/> where user_id = #{userId} and status = '0'
    </select>

    <select id="checkIdCardUnique" parameterType="String" resultMap="ExamCandidateResult">
        <include refid="selectExamCandidateVo"/> where id_card = #{idCard} limit 1
    </select>

    <insert id="insertExamCandidate" parameterType="ExamCandidate" useGeneratedKeys="true" keyProperty="candidateId">
        insert into exam_candidate(
            <if test="candidateName != null and candidateName != ''">candidate_name,</if>
            <if test="idCard != null and idCard != ''">id_card,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="gender != null and gender != ''">gender,</if>
            <if test="email != null and email != ''">email,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="userId != null">user_id,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="candidateName != null and candidateName != ''">#{candidateName},</if>
            <if test="idCard != null and idCard != ''">#{idCard},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="gender != null and gender != ''">#{gender},</if>
            <if test="email != null and email != ''">#{email},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="userId != null">#{userId},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateExamCandidate" parameterType="ExamCandidate">
        update exam_candidate
        <set>
            <if test="candidateName != null and candidateName != ''">candidate_name = #{candidateName},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null and gender != ''">gender = #{gender},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where candidate_id = #{candidateId}
    </update>

    <delete id="deleteExamCandidateById" parameterType="Long">
        delete from exam_candidate where candidate_id = #{candidateId}
    </delete>

    <delete id="deleteExamCandidateByIds" parameterType="Long">
        delete from exam_candidate where candidate_id in
        <foreach collection="array" item="candidateId" open="(" separator="," close=")">
            #{candidateId}
        </foreach>
    </delete>
</mapper>
